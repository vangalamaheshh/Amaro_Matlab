function test_compiler(varargin)

disp('Hello');
disp(isdeployed);
disp(path);

disp(length(varargin));

for i=1:length(varargin)
  disp(varargin{i});
  disp(ischar(varargin{i}));
end

% mcc -m -R -nojvm test_compiler

