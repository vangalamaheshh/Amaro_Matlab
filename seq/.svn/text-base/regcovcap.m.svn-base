function regcovcap(samples,params)

if ~exist('params','var'), params=[]; end

region_list = '/xchip/tcga_scratch/lawrence/db/chunks1e7.txt';
outstem = 'chunk1e7_cov_per_lane';

RegionCovPerLane(samples,region_list,outstem,params);
