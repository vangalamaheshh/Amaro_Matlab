function [matrix,matrixinfo] = nuc1

matrix = -5*ones(5,5);
for i=1:4, matrix(i,i)=1; end

matrixinfo.Name = 'NUC1';
matrixinfo.Scale = 0.2;
matrixinfo.Entropy = 0.5;
matrixinfo.ExpectedScore = -2;
matrixinfo.LowestScore = -5;
matrixinfo.HighestScore = 1;
matrixinfo.Order = 'ACGTN';
