function [T N] = load_isz_data(samples,P)
% [T N] = load_isz_data(samples,P)
%
% Mike Lawrence 2009

if ~exist('samples','var'), error('<samples> is required'); end
if ~exist('P','var'), P=[]; end

[T N] = load_TN_perlane_data(samples,P);
